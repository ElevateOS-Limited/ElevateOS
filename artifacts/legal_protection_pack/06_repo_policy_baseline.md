# Repo Policy Baseline

- Protect main branch (no force-push).
- Require PR review (>=1).
- Require status checks before merge.
- Require verified authorship and signed commits where supported.
- Enforce CODEOWNERS on sensitive paths.
